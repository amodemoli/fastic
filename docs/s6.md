#### ▲ STEP4 • SERV FRONTEND FILES (HTML,CSS,JS)

❝ **How Can I Serv My FrontEnd Files?**
you can serv your `.html`,`.css`,`.s` files, with `c.SendFile` method.
on `c.SendFile` method you can write your custom `.html` file path and link your css and js file to file. see example:

```go
// 🗁 main.go file contents
package basic

import (
	"github.com/amodemoli/fastic/core/fastic"
)

func main() {
	app := fastic.New()

	app.GET("/", func(c *fastic.Ctx) {
        // write this method and write target file path.
		c.SendFile("$_PATH/public/index.html")
	})

	app.Run(app.Handler)
}
```

for connect front end and backend you need ``RestApi`` or ``gRPC``, fastic framework dont have method for this but you can write easy api's with ``c.JSON`` or ``c.RawJSON`` full support of ``gRPC`` and ``RestFulApi`` coming soon in new version.

**᳃** More Documention Is Writing TNX For Reading, For Lastles Announcements Join To t.me/pludev
